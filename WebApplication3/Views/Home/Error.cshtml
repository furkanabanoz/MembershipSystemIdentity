@model List<string>
@{
    ViewData["Title"] = "Error";
}

<h3>Hata Meydana Geldi</h3>
<hr />

<div class="alert alert-danger">
    <ul>
        @foreach (var item in Model)
        {
            <li>@item</li>
        }
    </ul>
</div>
